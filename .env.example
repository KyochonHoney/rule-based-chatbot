# 복사해서 .env 파일로 만들어 사용하세요.

# --- 모델 경로 설정 ---
# docker-compose.yml의 볼륨 설정과 일치해야 합니다.
EMBED_ONNX_DIR=/models/emb
RERANK_ONNX_DIR=/models/rerank

# --- HuggingFace 모델명 (ONNX 변환 시 또는 Fallback 시 사용) ---
EMBED_MODEL=intfloat/multilingual-e5-small
RERANK_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2

# --- 재랭킹 조건 설정 ---
# 답변 신뢰도 임계값 (이 값보다 낮으면 재랭킹 또는 fallback)
CONFIDENCE_TH=0.52
# 1, 2위 후보 점수 격차가 이 값보다 작으면 재랭킹 수행
RERANK_COND_GAP=0.06

# --- 검색 관련 설정 ---
# 1차 하이브리드 검색에서 가져올 후보 수
TOPK=8
